import { Firebaseuserstatus } from './firebaseuserstatus';

describe('Firebaseuserstatus', () => {
  it('should create an instance', () => {
    expect(new Firebaseuserstatus()).toBeTruthy();
  });
});
